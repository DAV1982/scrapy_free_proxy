[tool.poetry]
name = "scraper"
version = "0.1.0"
description = ""
authors = ["DAV1982 <icesturm@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
